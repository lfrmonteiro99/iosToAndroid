<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        xmlns:tools="http://schemas.android.com/tools"
        tools:ignore="ProtectedPermissions" />
    <!-- #608 Tap to Wake: needed to wake the (app-dimmed) screen via PowerManager.ACQUIRE_CAUSES_WAKEUP -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- #636 Back Tap: foreground sensor service needs a foreground notification -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- TYPE_SENSOR foreground service (#770): Android 14+ requires the
         foreground-service permission that matches the service's
         foregroundServiceType. The AAPT in CI does not yet recognise the
         `sensor` type token, so we declare `connectedDevice` (the next closest
         hardware-sensor type the resource linker accepts) and the matching
         FOREGROUND_SERVICE_CONNECTED_DEVICE permission. The back-tap detector
         reads the accelerometer, so any hardware-sensor type is functionally
         equivalent here. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <application>
        <service
            android:name=".NotificationService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
        <!-- #636 Back Tap: detects double/triple taps on the device back via
             accelerometer + gyroscope. Runs as a foreground service so the
             detector survives the launcher being backgrounded. -->
        <service
            android:name=".TapSensorService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- #634: foreground service that polls UsageStats + AppOps for camera /
             mic / location access. Its own low-importance notification is shown via
             AccessEventsService.buildNotification(); it must NOT be typed as a
             dataSync/location/etc. foregroundServiceType because none of those
             exact types match "observe all three sensors" and a wrong type would
             require a matching permission on API 34+. Leaving it untyped keeps the
             API contract valid for a passive observer. -->
        <service
            android:name=".AccessEventsService"
            android:exported="false"
            android:stopWithTask="false" />

        <!-- #627 child issue: foreground monitor + Protected-Apps gate. -->
        <service
            android:name=".ForegroundMonitorService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/foreground_monitor_config" />
        </service>

        <!-- #627 child issue: transparent host for the Protected-Apps BiometricPrompt. -->
        <activity
            android:name=".ForegroundGuardActivity"
            android:theme="@style/Theme.TransparentGuard"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:exported="false" />

        <!-- #919: in-call UI provider, bound by Telecom once this app is
             selected as the default dialer (LauncherModule.requestDefaultDialer).
             android.telecom.IN_CALL_SERVICE_UI=true is required for Telecom to
             treat this as the app's OWN call-screen UI rather than a passive
             observer — without it, the installed dialer's screen would still
             be the one shown on top. exported=true + BIND_INCALL_SERVICE is
             the framework-mandated combination (Telecom itself is the only
             caller allowed to bind, enforced by the permission). -->
        <service
            android:name=".LauncherInCallService"
            android:permission="android.permission.BIND_INCALL_SERVICE"
            android:exported="true">
            <meta-data
                android:name="android.telecom.IN_CALL_SERVICE_UI"
                android:value="true" />
            <intent-filter>
                <action android:name="android.telecom.InCallService" />
            </intent-filter>
        </service>
    </application>
</manifest>
